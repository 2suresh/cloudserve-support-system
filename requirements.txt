# Core Dependencies
langchain>=0.1.0,<0.2.0
langgraph>=0.0.20,<0.1.0
langchain-community>=0.0.10,<0.1.0
langchain-openai>=0.0.7,<0.1.0

# Vector Database & Embeddings
chromadb==0.4.24
sentence-transformers>=3.0.0

# Data Processing
# Not imported anywhere in src/, evaluation/, scripts/, or tests/ -- kept only because the
# given requirements.txt lists them. Relaxed from exact pins (pandas==2.0.0 etc.) because
# those have no wheel for newer Python (e.g. 3.14) and fail to build from source; since
# nothing actually depends on the exact version, a range is strictly safer than an exact pin
# that works on some machines and not others.
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Web Framework
fastapi==0.104.0
uvicorn==0.24.0
pydantic>=2.4.0,<3.0.0
python-multipart==0.0.6
httpx<0.28  # starlette 0.27's TestClient uses httpx's deprecated app= shortcut, removed in 0.28

# LLM & API
openai>=1.10.0,<2.0.0
requests==2.31.0
python-dotenv==1.0.0
aiohttp==3.9.0

# Monitoring & Logging
prometheus-client==0.18.0
python-json-logger==2.0.7

# Database
# psycopg2-binary==2.9.9 has no prebuilt wheel for newer Python (e.g. 3.14) and fails to
# build from source there (uses a private CPython API removed in 3.14). Only needed at all
# if DATABASE_URL points at Postgres -- SQLite (the default) needs no database driver.
psycopg2-binary>=2.9.9
SQLAlchemy==2.0.0

# Testing & Quality
pytest==7.4.0
pytest-asyncio==0.21.0
pytest-cov==4.1.0
flake8==6.1.0
black==23.10.0

# Development
jupyter==1.0.0
ipython==8.15.0

# Utilities
tqdm==4.66.0
PyYAML==6.0.1
